/**
 * @deprecated Importar desde `@/components/patterns/EmptyState` (o el barrel
 * `@/components/patterns`). Se mantiene este re-export por compatibilidad con
 * los imports existentes.
 */
export { EmptyState } from './patterns/EmptyState';
